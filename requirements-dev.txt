# ============================================================================
# Development Dependencies
# ============================================================================
# Install with: pip install -r requirements-dev.txt
# This includes all production dependencies plus development tools

# Include production dependencies
-r requirements.txt

# ============================================================================
# Training & Experimentation (Not needed for deployment)
# ============================================================================
xgboost>=2.0.0               # Gradient boosting framework (baseline models)
imbalanced-learn>=0.11.0     # Handling imbalanced datasets (SMOTE, etc.)
albumentations>=1.3.0        # Image augmentation for training
scikit-image>=0.22.0         # Advanced image processing algorithms
mlflow>=2.9.0                # Experiment tracking, model registry, and UI dashboard
tqdm>=4.65.0                 # Progress bars for data loading

# ============================================================================
# Data Acquisition (Only needed for initial setup)
# ============================================================================
kagglehub>=0.2.0             # Modern Kaggle dataset download API
kaggle>=1.6.0                # Legacy Kaggle API (for authentication)

# ============================================================================
# Visualization Utilities
# ============================================================================
kaleido>=0.2.1               # Static image export for Plotly (notebook outputs)

# ============================================================================
# Code Quality & Linting
# ============================================================================
ruff>=0.1.0                  # Fast Python linter (replaces flake8, isort, etc.)
black>=23.0.0                # Code formatter for consistent style
pyright>=1.1.350             # Type checker (powers VS Code Pylance)

# ============================================================================
# Jupyter Notebook Development
# ============================================================================
jupyter>=1.0.0               # Jupyter notebook environment
notebook>=7.0.0              # Jupyter notebook interface
ipykernel>=6.25.0            # IPython kernel for Jupyter
nbstripout>=0.6.0            # Strip notebook outputs for version control
nbdime>=4.0.0                # Notebook diff and merge tools

# ============================================================================
# Notebook Workflow & Automation
# ============================================================================
jupytext>=1.16.0             # Notebook ↔ .py synchronization for version control
papermill>=2.4.0             # Parameterized notebook execution for testing
nbconvert>=7.0.0             # Notebook conversion utilities

# ============================================================================
# Testing & CI/CD
# ============================================================================
pytest>=7.4.0                # Testing framework
nbmake>=1.5.0                # Execute and test Jupyter notebooks
pytest-xdist>=3.5.0          # Parallel test execution
pytest-timeout>=2.2.0        # Timeout support for long-running tests
